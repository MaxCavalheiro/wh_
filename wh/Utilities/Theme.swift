//
//  Theme.swift
//  wh
//

import AppKit
import SwiftUI

enum Theme {
    /// Brand color: the same amber macOS uses for its "microphone in use" indicator.
    /// Currently applied to the record button only; meant to become the app's primary color.
    static let brand = Color(nsColor: .systemOrange)
}
